function wrapPromise(func, input) {
    return new Promise(function(resolve, reject) {
        var promiser = function(err, data) {
            if(err !== null) {
                return reject(err);
            }
            resolve(data);
        }
        func(input, promiser);
    });
}

module.exports = wrapPromise;
